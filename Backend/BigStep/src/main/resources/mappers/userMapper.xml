<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.bigstep.model.dao.UserDao">

	<insert id="insertUser" parameterType="User" keyProperty="id"
		useGeneratedKeys="true">
		INSERT INTO users (id, pw, name, nickname, address,
		height, weight, age, gender, isPublic)
		VALUE (#{id}, #{pw},
		#{name},
		#{nickname}, #{address}, #{height}, #{weight}, #{age}, #{gender},
		#{isPublic})
	</insert>

	<delete id="deleteUser" parameterType="String">
		DELETE FROM users WHERE id
		= #{id}
	</delete>

	<update id="updateUser" parameterType="String">
		UPDATE users SET nickname =
		#{nickname}, address = #{address}, height =
		#{height}, weight =
		#{weight}, age = #{age}, gender = #{gender}, isPublic = #{isPublic}
		WHERE id = #{id}
	</update>

	<select id="selectUsers">
		SELECT * from users
	</select>

	<select id="selectUser" parameterType="int">
		SELECT * FROM users Where
		id = #{id}
	</select>

</mapper>