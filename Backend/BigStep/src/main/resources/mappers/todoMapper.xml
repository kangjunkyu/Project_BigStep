<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.bigstep.model.dao.TodoDao">

	<insert id="insertTodo" parameterType="Todo"
		keyProperty="todoId" useGeneratedKeys="true">
		INSERT INTO todos(diaryid, userid,
		workout, start_hour, start_minute,
		end_hour,
		end_minute )
		VALUES (#{diaryid}, #{userid}, #{workout},
		#{start_hour},
		#{start_minute},
		#{end_hour}, #{end_minute})
	</insert>

	<delete id="deleteTodo" parameterType="int">
		DELETE FROM todos WHERE
		todoId = #{todoId}
	</delete>

	<update id="updateTodo" parameterType="Todo">
		UPDATE todos SET workout =
		#{workout}, start_hour = #{start_hour}, start_minute =
		#{start_minute}, end_hour = #{end_hour}, end_minute = #{end_hour}
		WHERE todoId =
		#{todoId}
	</update>

	<select id="selectTodos" parameterType="int" resultType="Todo">
		SELECT * FROM todos WHERE
		diaryId = #{diaryId}
	</select>

	<select id="selectTodo" parameterType="int" resultType="Todo">
		SELECT * FROM todos WHERE
		todoId = #{todoId}
	</select>
</mapper>